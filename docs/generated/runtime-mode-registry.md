# Runtime Mode Registry

> [!IMPORTANT]
> This file is generated by [`scripts/generate-runtime-mode-docs.mjs`](../../scripts/generate-runtime-mode-docs.mjs)
> from [`shared/runtime-mode-registry.ts`](../../shared/runtime-mode-registry.ts). Do not edit it by hand.
> Run `npm run generate:runtime-mode-docs` after changing the registry and
> `npm run check:runtime-mode-docs` to detect drift.

`authorityEngine` is the runtime mounted today. `intendedAuthorityEngine` appears only when the verified owner differs or a missing surface already has a selected family. Status details record known gaps and defects; they are not production-readiness claims.

A `capabilityKey` is only a binding to the public capability projection. It does not mean the mode is enabled, disabled, or healthy. Current availability remains owned by [`shared/public-capabilities.ts`](../../shared/public-capabilities.ts), its server implementation, and [`docs/LIVE_PRODUCT_STATUS.md`](../LIVE_PRODUCT_STATUS.md).

Legacy `migrationStatus` and `compatibility` metadata support the retired migration-oriented projection. They are intentionally omitted from this current authority view.

## Mode authority

| Mode | Category | Mounted owner | Intended owner | Participants | Reward | Record / replay | Orchestrator | Capability | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| <code>casual-pvp</code><br>Casual PvP | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>none</code> | <code>durable-pvp-history</code> | — | — | <code>match</code> |
| <code>direct-pvp-challenges</code><br>Player challenges | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>none</code> | <code>durable-pvp-history</code> | — | — | <code>match</code> |
| <code>ranked-shinobi-pvp</code><br>Ranked PvP | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history</code> | — | — | <code>match</code> |
| <code>village-guard-pvp-human</code><br>Village Guard human challenge | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history-plus-expiring-defense-marker</code> | — | — | <code>match</code> |
| <code>kage-succession-pvp</code><br>Kage succession duel | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history-plus-succession-record</code> | — | — | <code>match</code> |
| <code>clan-war-pvp-1v1</code><br>Clan War shinobi PvP 1v1 | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history</code> | <code>clan-war</code> | — | <code>match</code> |
| <code>clan-war-pvp-2v2</code><br>Clan War shinobi PvP 2v2 | <code>shinobi-pvp</code> | <code>tower</code> | <code>pvp</code> | <code>party</code> | <code>server-settled</code> | <code>bounded-terminal-command-ring</code> | <code>clan-war</code> | — | <code>match</code> |
| <code>generic-catalog-ai</code><br>Generic catalog AI | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>temporary-creator-ai</code><br>Temporary or creator AI | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>none</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>world-context-hunt-trails</code><br>World-context hunt trails | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>world-context-wanderers</code><br>World-context wanderers | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>world-context-village-crisis</code><br>World-context village crisis | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log-plus-global-crisis-ledger</code> | — | — | <code>match</code> |
| <code>world-context-hollow-gate-triad</code><br>Hollow Gate Reckoning shinobi 1v3 | <code>tower</code> | <code>tower</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-tower-run-plus-global-crisis-proof</code> | — | — | <code>match</code> |
| <code>world-context-hollow-gate-pets</code><br>Hollow Gate Reckoning companion 3v3 | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>party</code> | <code>parent-mode-settlement</code> | <code>expiring-showdown-turn-script-plus-global-crisis-proof</code> | — | — | <code>match</code> |
| <code>generic-apex-hunts</code><br>Generic Apex hunts | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>generic-explore-ambushes</code><br>Generic explore ambushes | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>generic-village-guard-raids</code><br>Generic village-guard raids | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>creator-event-practice</code><br>Creator-event practice fights | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>none</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>combat-missions-ed</code><br>E/D combat missions | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>combat-missions-cbas</code><br>C/B/A/S combat missions | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>story-battles</code><br>Story battles and bosses | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>academy-spar</code><br>Academy spar | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>dungeon-warden</code><br>Dungeon Warden | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-solo-session-log</code> | <code>dungeon</code> | — | <code>match</code> |
| <code>weekly-boss</code><br>Weekly Boss | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-capped</code> | <code>expiring-solo-session-log</code> | — | — | <code>match</code> |
| <code>endless</code><br>Endless | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-run-and-session</code> | — | — | <code>match</code> |
| <code>hollow-gate-shinobi</code><br>Hollow Gate shinobi | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-run-and-session</code> | <code>hollow-gate</code> | — | <code>match</code> |
| <code>anbu-infiltration</code><br>Anbu infiltration | <code>solo-pve</code> | <code>solo-pve</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-run-and-session-with-receipt</code> | — | <code>anbuInfiltration</code> | <code>match</code> |
| <code>battle-towers</code><br>Battle Towers | <code>tower</code> | <code>tower</code> | — | <code>solo-or-party</code> | <code>server-settled</code> | <code>expiring-tower-run-and-log</code> | — | — | <code>match</code> |
| <code>tower-party</code><br>Tower party | <code>tower</code> | <code>tower</code> | — | <code>party</code> | <code>server-settled</code> | <code>expiring-tower-run-and-log</code> | — | — | <code>match</code> |
| <code>endless-spire</code><br>Endless Spire | <code>tower</code> | <code>tower</code> | — | <code>party</code> | <code>server-capped</code> | <code>expiring-tower-run-and-log</code> | — | — | <code>match</code> |
| <code>clan-boss</code><br>Clan Boss | <code>tower</code> | <code>tower</code> | — | <code>solo-or-party</code> | <code>server-settled</code> | <code>expiring-tower-session-and-aggregate-standings</code> | — | <code>clanBoss</code> | <code>match</code> |
| <code>ranked-2v2</code><br>Ranked 2v2 | <code>shinobi-pvp</code> | <code>tower</code> | <code>pvp</code> | <code>party</code> | <code>server-settled</code> | <code>bounded-terminal-command-ring</code> | — | — | <code>match</code> |
| <code>tower-pvp</code><br>Team Arena 2v2 | <code>tower</code> | <code>tower</code> | — | <code>party</code> | <code>none</code> | <code>bounded-terminal-command-ring</code> | — | — | <code>match</code> |
| <code>world-sector-player-raid</code><br>Open-world sector raid | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history</code> | — | — | <code>match</code> |
| <code>sector-war-shinobi-human</code><br>Sector war shinobi combat | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>durable-pvp-history-plus-contest-receipt</code> | <code>sector-war</code> | <code>villageWar</code> | <code>match</code> |
| <code>sector-war-shinobi-garrison</code><br>Sector War shinobi garrison fallback | <code>shinobi-pvp</code> | <code>pvp</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-solo-session-log-plus-contest-receipt</code> | <code>sector-war</code> | <code>villageWar</code> | <code>match</code> |
| <code>village-war-mercenary</code><br>Village-War mercenary battle | <code>tower</code> | <code>tower</code> | — | <code>headless</code> | <code>server-settled</code> | <code>contest-receipt-only</code> | <code>sector-war</code> | <code>villageWar</code> | <code>match</code> |
| <code>sector-war-card</code><br>Sector war card combat | <code>card</code> | <code>chronicle</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>expiring-chronicle-projection</code> | <code>sector-war</code> | <code>villageWar</code> | <code>match</code> |
| <code>sector-war-pet</code><br>Sector war pet combat | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>derived-showdown-script</code> | <code>sector-war</code> | <code>villageWar</code> | <code>match</code> |
| <code>clan-war-tilecards</code><br>Clan War Tile Cards | <code>card</code> | <code>chronicle</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>expiring-chronicle-projection</code> | <code>clan-war</code> | — | <code>match</code> |
| <code>clan-war-pet</code><br>Clan War pet 1v1 and 2v2 | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>party</code> | <code>server-settled</code> | <code>derived-showdown-script</code> | <code>clan-war</code> | — | <code>match</code> |
| <code>pet-showdown-practice</code><br>Pet Showdown practice | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>none</code> | <code>expiring-showdown-turn-script</code> | — | — | <code>match</code> |
| <code>wild-pet-binding</code><br>Wild pet binding | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-showdown-turn-script</code> | — | — | <code>match</code> |
| <code>pet-wanderer-showdown</code><br>Natural sector pet wanderer | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>none</code> | <code>immutable-returned-showdown-script</code> | — | — | <code>match</code> |
| <code>pet-coliseum</code><br>Pet Coliseum | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>server-capped</code> | <code>expiring-showdown-turn-script</code> | — | — | <code>match</code> |
| <code>pet-ladder-showdown</code><br>Pet Ladder Showdown | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>asynchronous-defense</code> | <code>server-settled</code> | <code>returned-showdown-script</code> | — | — | <code>match</code> |
| <code>pet-warfront</code><br>Beastbound Warfront | <code>pet-tactical</code> | <code>pet-warfront</code> | — | <code>solo</code> | <code>server-capped</code> | <code>sealed-warfront-presentation-replay</code> | — | — | <code>match</code> |
| <code>tactical-arena</code><br>Tactical Arena (retired) | <code>pet-legacy</code> | — | <code>pet-warfront</code> | <code>solo</code> | <code>none</code> | <code>none</code> | — | — | <code>surface-gap</code> |
| <code>coop-tactical-arena</code><br>Co-op Beastbound Warfront | <code>pet-tactical</code> | <code>pet-warfront</code> | — | <code>party</code> | <code>none</code> | <code>client-warfront-replay-from-server-sealed-inputs</code> | — | — | <code>match</code> |
| <code>pet-ladder-warfront</code><br>Ranked Beastbound Warfront | <code>pet-tactical</code> | <code>pet-warfront</code> | — | <code>asynchronous-defense</code> | <code>server-settled</code> | <code>sealed-rite-formations-and-seed</code> | — | — | <code>match</code> |
| <code>pet-gauntlet</code><br>Pet Gauntlet | <code>pet-tactical</code> | <code>pet-gauntlet-grid</code> | — | <code>solo</code> | <code>server-capped</code> | <code>server-replayed-decision-transcript-and-receipt</code> | — | — | <code>match</code> |
| <code>pet-arena-ai-1v1</code><br>Pet Arena AI 1v1 (retired) | <code>pet-legacy</code> | — | <code>pet-cinematic-duel</code> | <code>solo</code> | <code>none</code> | <code>none</code> | — | — | <code>surface-gap</code> |
| <code>pet-arena-ai-2v2</code><br>Pet Arena AI 2v2 (retired) | <code>pet-legacy</code> | — | <code>pet-cinematic-duel</code> | <code>solo</code> | <code>none</code> | <code>none</code> | — | — | <code>surface-gap</code> |
| <code>pet-arena-ai-issued-token-compat</code><br>Pet Arena AI issued-token compatibility | <code>pet-legacy</code> | <code>pet-cinematic-duel</code> | — | <code>solo</code> | <code>server-capped</code> | <code>server-replayed-cinematic-input-log-with-receipt</code> | — | — | <code>match</code> |
| <code>pet-arena-pvp-1v1</code><br>Pet Arena live PvP 1v1 | <code>pet-legacy</code> | <code>pet-cinematic-duel</code> | — | <code>two-player</code> | <code>none</code> | <code>memory-only-server-replayed-lockstep-cinematic-log</code> | — | — | <code>match</code> |
| <code>pet-arena-pvp-2v2</code><br>Pet Arena live PvP 2v2 | <code>pet-legacy</code> | <code>pet-cinematic-duel</code> | — | <code>two-player</code> | <code>none</code> | <code>memory-only-server-replayed-lockstep-cinematic-log</code> | — | — | <code>match</code> |
| <code>pet-ranked-live</code><br>Pet ranked live queue | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>derived-showdown-script</code> | — | — | <code>match</code> |
| <code>pet-ranked-legacy-compat</code><br>Pet ranked legacy compatibility challenge | <code>pet-legacy</code> | <code>pet-showdown</code> | — | <code>two-player</code> | <code>server-settled</code> | <code>derived-showdown-script</code> | — | — | <code>match</code> |
| <code>hollow-gate-pet-showdown</code><br>Hollow Gate pet | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-showdown-turn-script-plus-parent-run-receipt</code> | <code>hollow-gate</code> | — | <code>match</code> |
| <code>dungeon-pet-cinematic</code><br>Dungeon pet | <code>pet-legacy</code> | <code>pet-cinematic-duel</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>server-replayed-cinematic-input-log-and-parent-run-receipt</code> | <code>dungeon</code> | — | <code>match</code> |
| <code>card-clash-freeplay</code><br>Card Clash | <code>card</code> | <code>chronicle</code> | — | <code>two-player</code> | <code>server-progression</code> | <code>expiring-chronicle-projection</code> | — | — | <code>match</code> |
| <code>card-clash-ai</code><br>Card Clash AI | <code>card</code> | <code>chronicle</code> | — | <code>solo</code> | <code>server-capped</code> | <code>expiring-chronicle-projection</code> | — | — | <code>match</code> |
| <code>echoes-of-war</code><br>Echoes of War campaign | <code>card</code> | <code>chronicle</code> | — | <code>solo</code> | <code>server-settled</code> | <code>expiring-chronicle-projection</code> | — | — | <code>match</code> |
| <code>dungeon-card</code><br>Dungeon Card seal | <code>card</code> | <code>chronicle</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-chronicle-projection-and-parent-run-proof-receipt</code> | <code>dungeon</code> | — | <code>match</code> |
| <code>hollow-gate-rift-card</code><br>Hollow Gate rift card ambush | <code>card</code> | <code>chronicle</code> | — | <code>solo</code> | <code>parent-mode-settlement</code> | <code>expiring-chronicle-projection-and-parent-run-proof-receipt</code> | <code>hollow-gate</code> | — | <code>match</code> |
| <code>celestial-first-pact</code><br>Celestial Tower: The First Pact | <code>pet-showdown</code> | <code>pet-showdown</code> | — | <code>solo</code> | <code>server-progression</code> | <code>durable-campaign-plus-expiring-showdown-turn-script</code> | — | — | <code>match</code> |

## Routes, transports, and client entries

HTTP route roles, paths, handlers, and non-HTTP gameplay transports are reproduced exactly as declared by the registry. Client entries and transport adapters are relative to `shinobij.client/src/`; transport handlers are relative to `api/`.

| Mode | HTTP route roles, paths, handlers, and caller requirement | Non-HTTP transport | Client entries |
| --- | --- | --- | --- |
| <code>casual-pvp</code> | <code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required) | — | <code>screens/Arena.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>direct-pvp-challenges</code> | <code>lifecycle</code> <code>/player/challenge</code> → <code>player/challenge</code> (client caller required)<br><code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required) | — | <code>App.tsx</code><br><code>screens/Arena.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>ranked-shinobi-pvp</code> | <code>start</code> <code>/pvp/ranked-queue</code> → <code>pvp/ranked-queue</code> (client caller required)<br><code>start</code> <code>/pvp/ranked-format-weapon</code> → <code>pvp/ranked-format-weapon</code> (client caller required)<br><code>leaderboard, record</code> <code>/ranked-season</code> → <code>ranked-season</code> (client caller required)<br><code>state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required) | — | <code>screens/Arena.tsx</code><br><code>screens/HallOfLegends.tsx</code><br><code>lib/player-ranked-authority.ts</code><br><code>lib/pvp-reward-claim.ts</code><br><code>components/RankedFormatWeaponPicker.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>village-guard-pvp-human</code> | <code>lifecycle</code> <code>/village-guard/challenge</code> → <code>village-guard/challenge</code> (client caller required)<br><code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required) | — | <code>screens/WorldMap.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>kage-succession-pvp</code> | <code>lifecycle, settlement</code> <code>/village/kage-challenge</code> → <code>village/kage-challenge</code> (client caller required)<br><code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required) | — | <code>App.tsx</code><br><code>screens/TownHall.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>clan-war-pvp-1v1</code> | <code>lifecycle</code> <code>/clan/war/challenge</code> → <code>clan/war/challenge</code> (client caller required)<br><code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required)<br><code>settlement</code> <code>/clan/war/report</code> → <code>clan/war/report</code> (client caller required) | — | <code>App.tsx</code><br><code>lib/clan-war-api.ts</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>clan-war-pvp-2v2</code> | <code>lifecycle</code> <code>/clan/war/challenge</code> → <code>clan/war/challenge</code> (client caller required)<br><code>start, settlement</code> <code>/clan/war/pvp-2v2</code> → <code>clan/war/pvp-2v2</code> (client caller required)<br><code>action</code> <code>/towers/pvp-action</code> → <code>towers/pvp-action</code> (client caller required)<br><code>state</code> <code>/towers/pvp-state</code> → <code>towers/pvp-state</code> (client caller required) | — | <code>App.tsx</code><br><code>lib/clan-war-api.ts</code><br><code>lib/clan-war-2v2-api.ts</code><br><code>screens/ClanWar2v2Battle.tsx</code><br><code>lib/tower-pvp-api.ts</code> |
| <code>generic-catalog-ai</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>temporary-creator-ai</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>world-context-hunt-trails</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required)<br><code>lifecycle</code> <code>/missions/hunt-trail</code> → <code>missions/hunt-trail</code> (client caller required) | — | <code>screens/HunterBoard.tsx</code><br><code>screens/WorldMap.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/world-hunt-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>world-context-wanderers</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>screens/WorldMap.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>world-context-village-crisis</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required)<br><code>lifecycle, observation</code> <code>/world-crisis</code> → <code>world-crisis</code> (client caller required) | — | <code>screens/WorldCrisis.tsx</code><br><code>lib/world-crisis.ts</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>world-context-hollow-gate-triad</code> | <code>lifecycle, observation</code> <code>/world-crisis-80</code> → <code>world-crisis-80</code> (client caller required)<br><code>start</code> <code>/world-crisis-80/combat-start</code> → <code>world-crisis-80/combat-start</code> (client caller required)<br><code>action</code> <code>/towers/action</code> → <code>towers/action</code> (client caller required)<br><code>state</code> <code>/towers/state</code> → <code>towers/state</code> (client caller required)<br><code>settlement</code> <code>/world-crisis-80/combat-settle</code> → <code>world-crisis-80/combat-settle</code> (client caller required) | — | <code>screens/WorldCrisis80.tsx</code><br><code>screens/BattleTowerFight.tsx</code><br><code>lib/world-crisis-80.ts</code><br><code>lib/towers-api.ts</code> |
| <code>world-context-hollow-gate-pets</code> | <code>lifecycle, observation</code> <code>/world-crisis-80</code> → <code>world-crisis-80</code> (client caller required)<br><code>start, action, state, settlement</code> <code>/pet/showdown</code> → <code>pet/showdown</code> (client caller required) | — | <code>screens/WorldCrisis80.tsx</code><br><code>components/PetShowdownBattle.tsx</code><br><code>lib/world-crisis-80.ts</code> |
| <code>generic-apex-hunts</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>screens/HunterBoard.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>generic-explore-ambushes</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>screens/WorldMap.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>generic-village-guard-raids</code> | <code>lifecycle</code> <code>/missions/raid-start</code> → <code>missions/raid-start</code> (client caller required)<br><code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>screens/WorldMap.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/ai-raid-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>creator-event-practice</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required) | — | <code>App.tsx</code><br><code>screens/WorldMap.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>combat-missions-ed</code> | <code>start</code> <code>/missions/combat-start</code> → <code>missions/combat-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/queue-combat-claim</code> → <code>missions/queue-combat-claim</code> (client caller required)<br><code>settlement</code> <code>/pve/fight-outcome</code> → <code>pve/fight-outcome</code> (client caller required) | — | <code>screens/Missions.tsx</code><br><code>screens/MissionArenaFight.tsx</code><br><code>lib/mission-combat-claim.ts</code><br><code>lib/pve-outcome-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>combat-missions-cbas</code> | <code>start</code> <code>/missions/combat-start</code> → <code>missions/combat-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/queue-combat-claim</code> → <code>missions/queue-combat-claim</code> (client caller required)<br><code>settlement</code> <code>/pve/fight-outcome</code> → <code>pve/fight-outcome</code> (client caller required) | — | <code>screens/Missions.tsx</code><br><code>screens/MissionArenaFight.tsx</code><br><code>lib/mission-combat-claim.ts</code><br><code>lib/pve-outcome-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>story-battles</code> | <code>start</code> <code>/story/boss-start</code> → <code>story/boss-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/story/settle</code> → <code>story/settle</code> (client caller required)<br><code>settlement</code> <code>/pve/fight-outcome</code> → <code>pve/fight-outcome</code> (client caller required) | — | <code>components/StoryBossFightHost.tsx</code><br><code>lib/story-combat-api.ts</code><br><code>lib/pve-outcome-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>academy-spar</code> | <code>start</code> <code>/story/spar-start</code> → <code>story/spar-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/story/settle</code> → <code>story/settle</code> (client caller required)<br><code>settlement</code> <code>/pve/fight-outcome</code> → <code>pve/fight-outcome</code> (client caller required) | — | <code>components/StoryBossFightHost.tsx</code><br><code>lib/story-combat-api.ts</code><br><code>lib/pve-outcome-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>dungeon-warden</code> | <code>start</code> <code>/missions/ai-fight-start</code> → <code>missions/ai-fight-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/missions/report-ai-fight</code> → <code>missions/report-ai-fight</code> (client caller required)<br><code>settlement</code> <code>/dungeon/run</code> → <code>dungeon/run</code> (client caller required) | — | <code>App.tsx</code><br><code>components/AiFightHost.tsx</code><br><code>lib/ai-fight-api.ts</code><br><code>lib/dungeon-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>weekly-boss</code> | <code>start, settlement</code> <code>/weekly-boss</code> → <code>weekly-boss</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required) | — | <code>screens/WeeklyBossArena.tsx</code><br><code>screens/WeeklyBossFight.tsx</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>endless</code> | <code>start</code> <code>/endless/wave-start</code> → <code>endless/wave-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/endless/run</code> → <code>endless/run</code> (client caller required) | — | <code>lib/endless-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>hollow-gate-shinobi</code> | <code>start</code> <code>/hollow-gate/combat-start</code> → <code>hollow-gate/combat-start</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required)<br><code>settlement</code> <code>/hollow-gate/combat-settle</code> → <code>hollow-gate/combat-settle</code> (client caller required) | — | <code>lib/hollow-gate-combat-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code><br><code>screens/Arena.tsx</code> |
| <code>anbu-infiltration</code> | <code>start, state, settlement, lifecycle</code> <code>/village/anbu-infiltration</code> → <code>village/anbu-infiltration</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required) | — | <code>features/anbuInfiltration/AnbuVaultRaid.tsx</code><br><code>lib/anbu-infiltration-api.ts</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>battle-towers</code> | <code>catalog</code> <code>/towers/floors</code> → <code>towers/floors</code> (client caller required)<br><code>lifecycle</code> <code>/towers/party</code> → <code>towers/party</code> (client caller required)<br><code>lifecycle, recovery</code> <code>/towers/join</code> → <code>towers/join</code> (client caller required)<br><code>start</code> <code>/towers/start</code> → <code>towers/start</code> (client caller required)<br><code>action</code> <code>/towers/action</code> → <code>towers/action</code> (client caller required)<br><code>state</code> <code>/towers/state</code> → <code>towers/state</code> (client caller required)<br><code>settlement</code> <code>/towers/settle</code> → <code>towers/settle</code> (client caller required)<br><code>recovery, state</code> <code>/towers/my-run</code> → <code>towers/my-run</code> (client caller required) | — | <code>lib/towers-api.ts</code><br><code>components/TowerReadyRoomPanel.tsx</code><br><code>screens/BattleTowerFight.tsx</code> |
| <code>tower-party</code> | <code>catalog</code> <code>/towers/floors</code> → <code>towers/floors</code> (client caller required)<br><code>lifecycle</code> <code>/towers/party</code> → <code>towers/party</code> (client caller required)<br><code>lifecycle, recovery</code> <code>/towers/join</code> → <code>towers/join</code> (client caller required)<br><code>start</code> <code>/towers/start</code> → <code>towers/start</code> (client caller required)<br><code>action</code> <code>/towers/action</code> → <code>towers/action</code> (client caller required)<br><code>state</code> <code>/towers/state</code> → <code>towers/state</code> (client caller required)<br><code>settlement</code> <code>/towers/settle</code> → <code>towers/settle</code> (client caller required)<br><code>recovery, state</code> <code>/towers/my-run</code> → <code>towers/my-run</code> (client caller required) | — | <code>lib/towers-api.ts</code><br><code>components/TowerReadyRoomPanel.tsx</code><br><code>screens/BattleTowerFight.tsx</code> |
| <code>endless-spire</code> | <code>lifecycle</code> <code>/towers/party</code> → <code>towers/party</code> (client caller required)<br><code>lifecycle, recovery</code> <code>/towers/join</code> → <code>towers/join</code> (client caller required)<br><code>start</code> <code>/towers/start</code> → <code>towers/start</code> (client caller required)<br><code>action</code> <code>/towers/action</code> → <code>towers/action</code> (client caller required)<br><code>state</code> <code>/towers/state</code> → <code>towers/state</code> (client caller required)<br><code>settlement</code> <code>/towers/settle</code> → <code>towers/settle</code> (client caller required)<br><code>recovery, state</code> <code>/towers/my-run</code> → <code>towers/my-run</code> (client caller required)<br><code>leaderboard</code> <code>/towers/spire-leaderboard</code> → <code>towers/spire-leaderboard</code> (client caller required) | — | <code>lib/towers-api.ts</code><br><code>components/TowerReadyRoomPanel.tsx</code><br><code>screens/BattleTowerFight.tsx</code> |
| <code>clan-boss</code> | <code>state, leaderboard</code> <code>/clan-boss/get</code> → <code>clan-boss/get</code> (client caller required)<br><code>lifecycle, communication</code> <code>/clan-boss/party</code> → <code>clan-boss/party</code> (client caller required)<br><code>lifecycle, recovery</code> <code>/towers/join</code> → <code>towers/join</code> (client caller required)<br><code>start</code> <code>/clan-boss/assault-start</code> → <code>clan-boss/assault-start</code> (client caller required)<br><code>action</code> <code>/towers/action</code> → <code>towers/action</code> (client caller required)<br><code>state</code> <code>/towers/state</code> → <code>towers/state</code> (client caller required)<br><code>recovery, state</code> <code>/towers/my-run</code> → <code>towers/my-run</code> (client caller required)<br><code>settlement</code> <code>/clan-boss/assault-settle</code> → <code>clan-boss/assault-settle</code> (client caller required) | — | <code>screens/ClanBoss.tsx</code><br><code>lib/clan-boss-api.ts</code><br><code>lib/towers-api.ts</code> |
| <code>ranked-2v2</code> | <code>lifecycle, start, settlement</code> <code>/pvp/ranked-2v2</code> → <code>pvp/ranked-2v2</code> (client caller required)<br><code>start</code> <code>/pvp/ranked-format-weapon</code> → <code>pvp/ranked-format-weapon</code> (client caller required)<br><code>action</code> <code>/towers/pvp-action</code> → <code>towers/pvp-action</code> (client caller required)<br><code>state</code> <code>/towers/pvp-state</code> → <code>towers/pvp-state</code> (client caller required) | — | <code>lib/ranked-2v2-api.ts</code><br><code>components/Ranked2v2Panel.tsx</code><br><code>components/RankedFormatWeaponPicker.tsx</code><br><code>features/arena/components/ArenaDistrictLobby.tsx</code><br><code>lib/tower-pvp-api.ts</code> |
| <code>tower-pvp</code> | <code>start</code> <code>/towers/pvp-queue</code> → <code>towers/pvp-queue</code> (client caller required)<br><code>action</code> <code>/towers/pvp-action</code> → <code>towers/pvp-action</code> (client caller required)<br><code>state</code> <code>/towers/pvp-state</code> → <code>towers/pvp-state</code> (client caller required)<br><code>settlement</code> <code>/towers/pvp-settle</code> → <code>towers/pvp-settle</code> (client caller required) | — | <code>lib/tower-pvp-api.ts</code><br><code>components/TowerPvpPanel.tsx</code><br><code>components/TeamArenaSection.tsx</code><br><code>features/arena/components/BattleArenaLobby.tsx</code> |
| <code>world-sector-player-raid</code> | <code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required)<br><code>lifecycle</code> <code>/player/attack</code> → <code>player/attack</code> (client caller required)<br><code>lifecycle</code> <code>/player/clear-attack</code> → <code>player/clear-attack</code> (client caller required)<br><code>lifecycle</code> <code>/player/challenge</code> → <code>player/challenge</code> (client caller required) | — | <code>App.tsx</code><br><code>screens/WorldMap.tsx</code><br><code>components/WorldSectorCommandPanel.tsx</code><br><code>lib/world-attack-claim.ts</code><br><code>lib/village-war-map.ts</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>sector-war-shinobi-human</code> | <code>start, state</code> <code>/pvp/session</code> → <code>pvp/session</code> (client caller required)<br><code>action</code> <code>/pvp/move</code> → <code>pvp/move</code> (client caller required)<br><code>communication</code> <code>/pvp/chat</code> → <code>pvp/chat</code> (client caller required)<br><code>lifecycle, observation</code> <code>/pvp/spectate</code> → <code>pvp/spectate</code> (client caller required)<br><code>state, observation</code> <code>/pvp/stream</code> → <code>pvp/stream</code> (client caller required)<br><code>record</code> <code>/pvp/combat-log</code> → <code>pvp/combat-log</code> (client caller required)<br><code>record</code> <code>/pvp/combat-history</code> → <code>pvp/combat-history</code> (client caller required)<br><code>settlement</code> <code>/pvp/claim-rewards</code> → <code>pvp/claim-rewards</code> (client caller required)<br><code>lifecycle, settlement</code> <code>/village/sector-war</code> → <code>village/sector-war</code> (client caller required) | — | <code>lib/village-war-map.ts</code><br><code>screens/WorldMap.tsx</code><br><code>screens/PvpBattleScreen.tsx</code><br><code>lib/pvp-combat-log-api.ts</code><br><code>lib/pvp-reward-claim.ts</code> |
| <code>sector-war-shinobi-garrison</code> | <code>start, state, settlement, lifecycle</code> <code>/village/sector-war</code> → <code>village/sector-war</code> (client caller required)<br><code>action</code> <code>/solo-pve/action</code> → <code>solo-pve/action</code> (client caller required)<br><code>state</code> <code>/solo-pve/state</code> → <code>solo-pve/state</code> (client caller required) | — | <code>lib/village-war-map.ts</code><br><code>lib/sector-war-garrison-api.ts</code><br><code>screens/VillageWarMap.tsx</code><br><code>screens/SectorWarGarrisonAssault.tsx</code><br><code>lib/solo-pve-api.ts</code><br><code>lib/solo-pve-arena-adapter.ts</code> |
| <code>village-war-mercenary</code> | <code>start, state, settlement</code> <code>/village/war-merc</code> → <code>village/war-merc</code> (client caller required)<br><code>start, state, settlement</code> <code>/sector/merc-roam</code> → <code>sector/merc-roam</code> (client caller required) | — | <code>lib/village-war-map.ts</code><br><code>lib/merc-ai.ts</code><br><code>lib/merc-roam-client.ts</code> |
| <code>sector-war-card</code> | <code>start, action, state, settlement</code> <code>/village/sector-card</code> → <code>village/sector-card</code> (client caller required) | — | <code>screens/SectorWarCardBattle.tsx</code> |
| <code>sector-war-pet</code> | <code>start, state, settlement</code> <code>/village/sector-pet</code> → <code>village/sector-pet</code> (client caller required) | — | <code>screens/SectorWarPetBattle.tsx</code><br><code>lib/village-war-map.ts</code> |
| <code>clan-war-tilecards</code> | <code>lifecycle</code> <code>/clan/war/challenge</code> → <code>clan/war/challenge</code> (client caller required)<br><code>start, action, state, settlement</code> <code>/clan/war/tilecards</code> → <code>clan/war/tilecards</code> (client caller required) | — | <code>screens/ClanWarTileCardDuel.tsx</code><br><code>lib/clan-war-api.ts</code> |
| <code>clan-war-pet</code> | <code>lifecycle</code> <code>/clan/war/challenge</code> → <code>clan/war/challenge</code> (client caller required)<br><code>start, state, settlement</code> <code>/clan/war/pet</code> → <code>clan/war/pet</code> (client caller required) | — | <code>App.tsx</code><br><code>screens/ClanWarPetBattle.tsx</code><br><code>lib/clan-war-pet-api.ts</code><br><code>lib/clan-war-api.ts</code> |
| <code>pet-showdown-practice</code> | <code>start, action, state, settlement</code> <code>/pet/showdown</code> → <code>pet/showdown</code> (client caller required) | — | <code>screens/PetShowdown.tsx</code><br><code>lib/pet-showdown-api.ts</code> |
| <code>wild-pet-binding</code> | <code>start, action, state, settlement</code> <code>/pet/wild-binding</code> → <code>pet/wild-binding</code> (client caller required) | — | <code>components/WildPetBinding.tsx</code><br><code>lib/wild-binding-api.ts</code> |
| <code>pet-wanderer-showdown</code> | <code>start, recovery</code> <code>/pet/battle-start</code> → <code>pet/battle-start</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required) | — | <code>screens/WorldMap.tsx</code><br><code>screens/PetArena.tsx</code><br><code>data/pet-arena-opponents.ts</code> |
| <code>pet-coliseum</code> | <code>start, action, state, settlement</code> <code>/pet/showdown</code> → <code>pet/showdown</code> (client caller required) | — | <code>screens/PetArena.tsx</code><br><code>screens/PetShowdown.tsx</code><br><code>lib/pet-showdown-api.ts</code> |
| <code>pet-ladder-showdown</code> | <code>start, state, settlement</code> <code>/pet-ladder</code> → <code>pet-ladder/ladder</code> (client caller required) | — | <code>lib/pet-ladder-client.ts</code><br><code>screens/HallOfLegends.tsx</code> |
| <code>pet-warfront</code> | <code>start, state</code> <code>/pet/warfront-start</code> → <code>pet/warfront-start</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required) | — | <code>screens/PetArena.tsx</code> |
| <code>tactical-arena</code> | — | — | <code>screens/PetArena.tsx</code> |
| <code>coop-tactical-arena</code> | <code>start, state, lifecycle</code> <code>/arena/lobby</code> → <code>arena/lobby</code> (client caller required) | — | <code>components/ArenaCoopLobby.tsx</code><br><code>screens/PetArena.tsx</code> |
| <code>pet-ladder-warfront</code> | <code>start, state, settlement</code> <code>/pet-ladder</code> → <code>pet-ladder/ladder</code> (client caller required) | — | <code>lib/pet-ladder-client.ts</code><br><code>screens/PetLadder.tsx</code><br><code>screens/HallOfLegends.tsx</code> |
| <code>pet-gauntlet</code> | <code>start, state, settlement</code> <code>/pet/gauntlet</code> → <code>pet/gauntlet</code> (client caller required) | — | <code>components/PetGauntlet.tsx</code><br><code>lib/pet-gauntlet-api.ts</code><br><code>screens/PetArena.tsx</code> |
| <code>pet-arena-ai-1v1</code> | — | — | <code>screens/PetArena.tsx</code> |
| <code>pet-arena-ai-2v2</code> | — | — | <code>screens/PetArena.tsx</code> |
| <code>pet-arena-ai-issued-token-compat</code> | <code>recovery</code> <code>/pet/battle-start</code> → <code>pet/battle-start</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required) | — | <code>screens/PetArena.tsx</code> |
| <code>pet-arena-pvp-1v1</code> | — | <code>start, action, state, settlement</code> <code>socket.io</code> <code>petduel:*</code> → <code>_realtime/pet-duel-socket</code> / <code>lib/pet-duel-transport.ts</code> (memory-only) | <code>screens/PetArena.tsx</code><br><code>components/PetDuelLiveHost.tsx</code><br><code>lib/pet-duel-transport.ts</code> |
| <code>pet-arena-pvp-2v2</code> | — | <code>start, action, state, settlement</code> <code>socket.io</code> <code>petduel:*</code> → <code>_realtime/pet-duel-socket</code> / <code>lib/pet-duel-transport.ts</code> (memory-only) | <code>screens/PetArena.tsx</code><br><code>components/PetDuelLiveHost.tsx</code><br><code>lib/pet-duel-transport.ts</code> |
| <code>pet-ranked-live</code> | <code>lifecycle</code> <code>/pvp/pet-ranked-queue</code> → <code>pvp/pet-ranked-queue</code> (client caller required)<br><code>start</code> <code>/pet/ranked-start</code> → <code>pet/ranked-start</code> (client caller required)<br><code>state, observation</code> <code>/pet/ranked-watch</code> → <code>pet/ranked-watch</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required) | — | <code>screens/PetLadder.tsx</code><br><code>components/PetLadderQueuePanel.tsx</code><br><code>lib/pet-ranked-queue-api.ts</code><br><code>lib/pet-ranked-watch-api.ts</code> |
| <code>pet-ranked-legacy-compat</code> | <code>lifecycle</code> <code>/player/challenge</code> → <code>player/challenge</code> (client caller required)<br><code>start, state</code> <code>/pet/ranked-start</code> → <code>pet/ranked-start</code> (client caller required)<br><code>state, observation</code> <code>/pet/ranked-watch</code> → <code>pet/ranked-watch</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required) | — | <code>screens/Arena.tsx</code><br><code>screens/PetArena.tsx</code> |
| <code>hollow-gate-pet-showdown</code> | <code>lifecycle</code> <code>/hollow-gate/combat-start</code> → <code>hollow-gate/combat-start</code> (client caller required)<br><code>start, action, state</code> <code>/pet/showdown</code> → <code>pet/showdown</code> (client caller required)<br><code>settlement</code> <code>/hollow-gate/combat-settle</code> → <code>hollow-gate/combat-settle</code> (client caller required) | — | <code>lib/hollow-gate-combat-api.ts</code><br><code>components/HollowGatePetFight.tsx</code><br><code>lib/pet-showdown-api.ts</code> |
| <code>dungeon-pet-cinematic</code> | <code>start, state</code> <code>/pet/battle-start</code> → <code>pet/battle-start</code> (client caller required)<br><code>settlement</code> <code>/pet/battle-result</code> → <code>pet/battle-result</code> (client caller required)<br><code>settlement</code> <code>/dungeon/run</code> → <code>dungeon/run</code> (client caller required) | — | <code>screens/Dungeon.tsx</code><br><code>lib/dungeon-pet-authority.ts</code><br><code>lib/dungeon-api.ts</code> |
| <code>card-clash-freeplay</code> | <code>start, lifecycle</code> <code>/card-clash/queue</code> → <code>card-clash/queue</code> (client caller required)<br><code>action, state, settlement</code> <code>/card-clash/match</code> → <code>card-clash/match</code> (client caller required) | — | <code>screens/CardClashFreePlay.tsx</code><br><code>lib/free-play-queue-client.ts</code> |
| <code>card-clash-ai</code> | <code>start</code> <code>/card-clash/ai-start</code> → <code>card-clash/ai-start</code> (client caller required)<br><code>action, state, settlement</code> <code>/card-clash/ai-move</code> → <code>card-clash/ai-move</code> (client caller required) | — | <code>screens/CardClashDuel.tsx</code><br><code>lib/chronicle-duel.ts</code> |
| <code>echoes-of-war</code> | <code>start</code> <code>/card-clash/ai-start</code> → <code>card-clash/ai-start</code> (client caller required)<br><code>action, state, settlement</code> <code>/card-clash/ai-move</code> → <code>card-clash/ai-move</code> (client caller required) | — | <code>screens/EchoesOfWar.tsx</code><br><code>screens/CardClashDuel.tsx</code><br><code>lib/chronicle-duel.ts</code> |
| <code>dungeon-card</code> | <code>start</code> <code>/card-clash/ai-start</code> → <code>card-clash/ai-start</code> (client caller required)<br><code>action, state, settlement</code> <code>/card-clash/ai-move</code> → <code>card-clash/ai-move</code> (client caller required)<br><code>settlement</code> <code>/dungeon/run</code> → <code>dungeon/run</code> (client caller required) | — | <code>screens/Dungeon.tsx</code><br><code>screens/CardClashDuel.tsx</code><br><code>lib/chronicle-duel.ts</code><br><code>lib/dungeon-api.ts</code> |
| <code>hollow-gate-rift-card</code> | <code>start, recovery</code> <code>/hollow-gate/card-start</code> → <code>hollow-gate/card-start</code> (client caller required)<br><code>action, state, settlement</code> <code>/card-clash/ai-move</code> → <code>card-clash/ai-move</code> (client caller required)<br><code>settlement</code> <code>/hollow-gate/card-settle</code> → <code>hollow-gate/card-settle</code> (client caller required) | — | <code>screens/CardClashDuel.tsx</code><br><code>lib/chronicle-duel.ts</code><br><code>lib/hollow-gate-card-api.ts</code> |
| <code>celestial-first-pact</code> | <code>state, lifecycle, settlement</code> <code>/first-pact/state</code> → <code>first-pact/state</code> (client caller required)<br><code>start, action, state, settlement</code> <code>/pet/showdown</code> → <code>pet/showdown</code> (client caller required) | — | <code>screens/FirstPact.tsx</code><br><code>lib/first-pact-api.ts</code><br><code>lib/first-pact-world.ts</code><br><code>lib/pet-showdown-api.ts</code> |

## Intentional engine separations

These relationships prevent a shared rule or adapter from being mistaken for permission to merge product runtimes.

| Engine | Intentionally separate from |
| --- | --- |
| <code>pvp</code> | <code>solo-pve</code><br><code>tower</code> |
| <code>solo-pve</code> | <code>pvp</code><br><code>tower</code> |
| <code>tower</code> | <code>pvp</code><br><code>solo-pve</code> |
| <code>pet-showdown</code> | <code>pet-warfront</code><br><code>pet-gauntlet-grid</code><br><code>pet-cinematic-duel</code><br><code>legacy-pet-duel</code><br><code>client-local-pet-duel</code> |
| <code>pet-warfront</code> | <code>pet-showdown</code><br><code>pet-gauntlet-grid</code><br><code>pet-cinematic-duel</code><br><code>legacy-pet-duel</code><br><code>client-local-pet-duel</code> |
| <code>chronicle</code> | <code>pvp</code><br><code>solo-pve</code><br><code>tower</code><br><code>pet-showdown</code><br><code>pet-warfront</code><br><code>pet-gauntlet-grid</code> |
| <code>pet-gauntlet-grid</code> | <code>pet-showdown</code><br><code>pet-warfront</code><br><code>chronicle</code><br><code>pet-cinematic-duel</code><br><code>legacy-pet-duel</code><br><code>client-local-pet-duel</code> |
| <code>pet-cinematic-duel</code> | <code>pet-showdown</code><br><code>pet-warfront</code><br><code>pet-gauntlet-grid</code><br><code>legacy-pet-duel</code><br><code>client-local-pet-duel</code> |
| <code>legacy-pet-duel</code> | <code>pet-showdown</code><br><code>pet-warfront</code><br><code>pet-gauntlet-grid</code><br><code>pet-cinematic-duel</code><br><code>client-local-pet-duel</code> |
| <code>client-local-pet-duel</code> | <code>pet-showdown</code><br><code>pet-warfront</code><br><code>pet-gauntlet-grid</code><br><code>pet-cinematic-duel</code><br><code>legacy-pet-duel</code> |

## Declared orchestration owners

Orchestrators admit, coordinate, or settle a mode; they are not combat engines.

| Orchestrator | Modes |
| --- | --- |
| <code>sector-war</code> | <code>sector-war-shinobi-human</code><br><code>sector-war-shinobi-garrison</code><br><code>village-war-mercenary</code><br><code>sector-war-card</code><br><code>sector-war-pet</code> |
| <code>clan-war</code> | <code>clan-war-pvp-1v1</code><br><code>clan-war-pvp-2v2</code><br><code>clan-war-tilecards</code><br><code>clan-war-pet</code> |
| <code>dungeon</code> | <code>dungeon-warden</code><br><code>dungeon-pet-cinematic</code><br><code>dungeon-card</code> |
| <code>hollow-gate</code> | <code>hollow-gate-shinobi</code><br><code>hollow-gate-pet-showdown</code><br><code>hollow-gate-rift-card</code> |

## Open gaps and mismatches

| Mode | Status | Mounted owner | Intended owner | Detail |
| --- | --- | --- | --- | --- |
| <code>tactical-arena</code> | <code>surface-gap</code> | — | <code>pet-warfront</code> | Retired by the Beastbound Warfront consolidation. This route-less compatibility ID cannot launch; solo, co-op, and offline ranked play use Beastbound Warfront. No standalone Tactical surface is planned. |
| <code>pet-arena-ai-1v1</code> | <code>surface-gap</code> | — | <code>pet-cinematic-duel</code> | New user-picked cinematic AI admission is retired fail-closed. The separate issued-token compatibility row owns exact recovery and settlement of an already-active pre-cutover seal; paid Pet Coliseum progression belongs only to Showdown. |
| <code>pet-arena-ai-2v2</code> | <code>surface-gap</code> | — | <code>pet-cinematic-duel</code> | New user-picked cinematic AI admission is retired fail-closed. The separate issued-token compatibility row owns exact recovery and settlement of an already-active pre-cutover seal; paid Pet Coliseum progression belongs only to Showdown. |

## Public capability bindings

Only explicit bindings are listed. An unlisted mode has no registry binding; that absence is not an availability statement.

| Mode | Capability key |
| --- | --- |
| <code>anbu-infiltration</code> | <code>anbuInfiltration</code> |
| <code>clan-boss</code> | <code>clanBoss</code> |
| <code>sector-war-shinobi-human</code> | <code>villageWar</code> |
| <code>sector-war-shinobi-garrison</code> | <code>villageWar</code> |
| <code>village-war-mercenary</code> | <code>villageWar</code> |
| <code>sector-war-card</code> | <code>villageWar</code> |
| <code>sector-war-pet</code> | <code>villageWar</code> |
