org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental caches cannot span two drive roots on Windows ("this and
# base files have different roots"), which is exactly the case when the project
# is built through a `subst` drive while the pub cache stays on C:. The shell's
# Kotlin is a few plugin files, so full compiles cost seconds.
kotlin.incremental=false
