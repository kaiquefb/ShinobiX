name: CI

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Every gate below pipes to `tee` to retain evidence. GitHub's DEFAULT shell for
# `run:` is `bash -e {0}` with NO pipefail, so a piped step reports `tee`'s exit
# status and a CRASHING gate is recorded as green — check:tooling-handoffs did
# exactly that from 2026-08-18 to 08-20. Naming the shell explicitly gets
# `bash --noprofile --norc -eo pipefail {0}`, which is what makes these gates real.
defaults:
  run:
    shell: bash

env:
  VITE_SENTRY_DSN: https://public@example.invalid/1
  VITE_SENTRY_RELEASE: ci
  VITE_BUILD_COMMIT: ${{ github.sha }}

jobs:
  server_contracts_matrix:
    name: CI / server-contracts / ${{ matrix.shard }}
    runs-on: ubuntu-latest
    # The suite is sharded because the single job kept reaching the ceiling the
    # comment below warned about: `Root tests and executable contracts` alone ran
    # 17m of an 18m job on a fast runner and 29m05s on a slow one, where it was
    # CANCELLED -- red on timing, with the trailing contract steps never executed.
    # Every other step in the old job measured 0m, so the suite was the whole cost.
    # Splitting it three ways follows that instruction instead of raising 29 again;
    # the aggregator below keeps `CI / server-contracts` as the required check.
    # 29, matching the rest of the workflow, because the SHARD is what shrank --
    # not the ceiling. Measured on run 32454050576: shards 2 and 3 take 3.1m and
    # 3.6m, shard 1 takes 15.9m. Runner speed varies by ~1.7x here (the old job
    # measured 17m and 29m for identical work), and 15.9 x 1.7 is 27m, which a
    # 20m shard ceiling would cancel. The headroom is real now regardless: 16m of
    # work under a 29m ceiling, where the old job carried 29m of work under it.
    #
    # Shard 1 is heavy because ONE test is 9.6 minutes -- `streamed render path
    # === full-auto server path (autobuy, all policies + stances)` -- with the
    # other Warfront parity sims adding ~5m around it. node:test runs a file's
    # tests sequentially, so no file-level split can go below that; rebalancing
    # the deal would move the cost, not remove it. Making that suite cheaper is
    # its owner's call, not this split's.
    timeout-minutes: 29
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/server-contracts-${{ matrix.shard }}
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/server-contracts-${{ matrix.shard }}/provenance.txt
      - name: Install root dependencies
        run: npm ci 2>&1 | tee .ci-evidence/server-contracts-${{ matrix.shard }}/root-install.log
      - name: Install client dependencies for shared tests
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/server-contracts-${{ matrix.shard }}/client-install.log
      - name: Root tests and executable contracts
        env:
          TEST_SHARD_INDEX: ${{ matrix.shard }}
          TEST_SHARD_TOTAL: 3
        run: npm run test:ci 2>&1 | tee .ci-evidence/server-contracts-${{ matrix.shard }}/root-tests.log
      - name: Retain server contract shard evidence
        if: always()
        # Evidence is diagnostic, never a release gate: nothing downstream downloads it,
        # and a refusal from GitHub's artifact blob service (a 403 on FinalizeArtifact
        # cancelled three consecutive main rollouts on 2026-09-08 while every test was
        # green) must not fail the job that produced the evidence.
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-server-contracts-shard${{ matrix.shard }}-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/server-contracts-${{ matrix.shard }}/
          if-no-files-found: warn
          retention-days: 14

  server_contracts:
    name: CI / server-contracts
    needs: server_contracts_matrix
    if: ${{ always() }}
    runs-on: ubuntu-latest
    # Keeps the required check name stable while the suite itself runs in the
    # matrix above. The executable contracts stay here: each measured 0m, so they
    # cost nothing once, and running them per-shard would triple them for nothing.
    #
    # The reasoning from the original split still stands: a timeout cancel is WORSE
    # than a slow run, because the trailing steps silently never execute and gate
    # nothing while main shows red. If a job here approaches its ceiling, split it
    # again rather than raising the number.
    timeout-minutes: 15
    # (No job-level Sentry env block: the split hoisted VITE_SENTRY_DSN /
    # VITE_SENTRY_RELEASE / VITE_BUILD_COMMIT to the workflow `env:` above, so
    # every job exercises the same Sentry-enabled bundle path Railway builds
    # rather than only this one. The dummy DSN is public-format test data and
    # sends nothing during build or smoke.)
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/server-contracts
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/server-contracts/provenance.txt
      - name: Install root dependencies
        run: npm ci 2>&1 | tee .ci-evidence/server-contracts/root-install.log
      - name: Deployment topology contract
        run: npm run check:deployment 2>&1 | tee .ci-evidence/server-contracts/deployment.log
      - name: Rollback compatibility contract
        run: npm run check:rollback-readiness 2>&1 | tee .ci-evidence/server-contracts/rollback.log
      - name: Mission eligibility catalog
        run: npm run test:mission-eligibility 2>&1 | tee .ci-evidence/server-contracts/mission-eligibility.log
      - name: Release asset references and decodability
        run: npm run test:release-assets 2>&1 | tee .ci-evidence/server-contracts/release-assets.log
      - name: Pet breeding probability audit
        run: npm run test:pet-breeding-odds 2>&1 | tee .ci-evidence/server-contracts/pet-breeding-odds.log
      - name: Tooling handoff drift
        run: npm run check:tooling-handoffs 2>&1 | tee .ci-evidence/server-contracts/tooling-handoffs.log
      - name: Require every test shard
        env:
          SHARDS: ${{ needs.server_contracts_matrix.result }}
        run: |
          printf 'server-contracts-shards=%s\n' "$SHARDS" | tee .ci-evidence/server-contracts/shards.log
          test "$SHARDS" = success
      - name: Retain server contract evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-server-contracts-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/server-contracts/
          if-no-files-found: warn
          retention-days: 14
  server_build_security:
    name: CI / server-build-security
    runs-on: ubuntu-latest
    # 15 was enough until the audit step learned to retry (see below). A single
    # failing audit attempt cost 5m of npm's own internal retrying, so three of
    # them plus install, backup tests and the server build no longer fit.
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/server-build-security
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/server-build-security/provenance.txt
      - name: Install root dependencies
        run: npm ci 2>&1 | tee .ci-evidence/server-build-security/root-install.log
      - name: Backup and restore safety
        run: npm run test:backup 2>&1 | tee .ci-evidence/server-build-security/backup-tests.log
      - name: Root dependency audit
        # The gate is unchanged: one high-severity advisory still fails the job,
        # on the first attempt, with no retry. What is new is the handling of npm
        # answering with no verdict at all. On 2026-09-04 the audit endpoint
        # returned `400 Bad Request - Invalid package tree` for the very lockfile
        # it had accepted 100 minutes earlier on the #118 merge; the step failed,
        # and because every e2e suite, concurrency-smoke, release-artifact and
        # release-certification hang off this job, the whole suite went red and
        # Railway skipped the deploy of a commit whose code was fine. Re-running
        # the identical commit passed all 19 jobs. A registry blip should not
        # cost a production deploy, and retrying costs nothing when npm is well.
        run: |
          log=.ci-evidence/server-build-security/root-audit.log
          attempt_out="$(mktemp)"
          : > "$log"
          for attempt in 1 2 3; do
            printf '=== attempt %s of 3 ===\n' "$attempt" | tee -a "$log"
            if npm audit --audit-level=high > "$attempt_out" 2>&1; then
              tee -a "$log" < "$attempt_out"
              exit 0
            fi
            tee -a "$log" < "$attempt_out"
            # A real advisory is a verdict. Fail on it now rather than burning
            # 10 more minutes to be told the same thing twice.
            if ! grep -qE 'audit endpoint returned an error|ECONNRESET|ETIMEDOUT|ENOTFOUND|EAI_AGAIN|ERR_SOCKET_TIMEOUT' "$attempt_out"; then
              echo "::error::npm audit reported a high-severity advisory."
              exit 1
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "::warning::npm audit endpoint errored on attempt $attempt/3; retrying in 30s."
              sleep 30
            fi
          done
          echo "::error::npm audit endpoint returned no verdict in 3 attempts."
          exit 1
      - name: Build server once
        run: npm run build:server 2>&1 | tee .ci-evidence/server-build-security/server-build.log
      - name: Package immutable server artifact
        run: |
          mkdir -p .ci-artifacts/server
          tar -czf .ci-artifacts/server/server-dist.tgz dist
          cd .ci-artifacts/server
          sha256sum server-dist.tgz > server-dist.tgz.sha256
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\nnode=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$(node -v)" > provenance.txt
      - name: Upload server artifact
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: server-dist-${{ github.sha }}-${{ github.run_id }}
          overwrite: true
          path: .ci-artifacts/server/
          if-no-files-found: error
          retention-days: 14
      - name: Retain server build evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-server-build-security-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/server-build-security/
          if-no-files-found: warn
          retention-days: 14

  client_quality:
    name: CI / client-quality
    runs-on: ubuntu-latest
    # Raised with the audit retry below, on the same reasoning as
    # server-build-security: three failing audit attempts are ~15m on their own.
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/client-quality
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/client-quality/provenance.txt
      - name: Install root tooling dependencies
        run: npm ci 2>&1 | tee .ci-evidence/client-quality/root-install.log
      - name: Install client dependencies
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/client-install.log
      - name: Certify shipped pet rigs and animations
        run: npm run qa:pet-models --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/pet-models.log
      - name: Certify all Warfront and Rally LODs
        run: npm run check:warfront-pet-lods --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/pet-lods.log
      - name: Certify all Warfront impostors
        run: npm run check:warfront-pet-impostors --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/pet-impostors.log
      - name: Client lint
        run: npm run lint --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/lint.log
      - name: Story-content, TypeScript, and production client build
        run: npm run build --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/client-build.log
      - name: Build-size gate
        # This step once carried its own `shell: bash`, because `| tee` under the
        # default pipefail-less shell reported success while the gate printed its
        # own failure. It mattered — an over-budget initial graph reached main
        # under a green check, and the next thing to notice was the Railway image
        # build, which runs `npm run build` (sizecheck included) unpiped and simply
        # stops deploying. That per-step override was the right call when it was
        # the only one; the workflow-level `defaults.run.shell` above now does the
        # same for all 42 piped steps, so keeping it here would only imply the
        # other steps lack the protection. The history is the point of this note.
        run: npm run sizecheck 2>&1 | tee .ci-evidence/client-quality/build-size.log
      - name: Visual-baseline size gate
        run: npm run test:e2e:visual:size --prefix shinobij.client 2>&1 | tee .ci-evidence/client-quality/visual-size.log
      - name: Client dependency audit
        # Same contract as the root audit in server-build-security: a real
        # advisory fails immediately, npm failing to answer is retried. This one
        # happened to pass on 2026-09-04 while the root audit did not, which is
        # the clearest evidence available that the failure was the registry's
        # and not the lockfile's.
        run: |
          log=.ci-evidence/client-quality/client-audit.log
          attempt_out="$(mktemp)"
          : > "$log"
          for attempt in 1 2 3; do
            printf '=== attempt %s of 3 ===\n' "$attempt" | tee -a "$log"
            if npm audit --prefix shinobij.client --audit-level=high > "$attempt_out" 2>&1; then
              tee -a "$log" < "$attempt_out"
              exit 0
            fi
            tee -a "$log" < "$attempt_out"
            if ! grep -qE 'audit endpoint returned an error|ECONNRESET|ETIMEDOUT|ENOTFOUND|EAI_AGAIN|ERR_SOCKET_TIMEOUT' "$attempt_out"; then
              echo "::error::npm audit reported a high-severity advisory."
              exit 1
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "::warning::npm audit endpoint errored on attempt $attempt/3; retrying in 30s."
              sleep 30
            fi
          done
          echo "::error::npm audit endpoint returned no verdict in 3 attempts."
          exit 1
      - name: Package immutable client artifact
        run: |
          mkdir -p .ci-artifacts/client
          tar -czf .ci-artifacts/client/client-dist.tgz shinobij.client/dist
          cd .ci-artifacts/client
          sha256sum client-dist.tgz > client-dist.tgz.sha256
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\nnode=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$(node -v)" > provenance.txt
      - name: Upload client artifact
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: client-dist-${{ github.sha }}-${{ github.run_id }}
          overwrite: true
          path: .ci-artifacts/client/
          if-no-files-found: error
          retention-days: 14
      - name: Retain client quality evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-client-quality-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/client-quality/
          if-no-files-found: warn
          retention-days: 14

  release_artifact:
    name: CI / release-artifact
    needs: [server_build_security, client_quality]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/release-artifact
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/release-artifact/provenance.txt
      - name: Require successful build inputs
        if: ${{ needs.server_build_security.result != 'success' || needs.client_quality.result != 'success' }}
        run: |
          printf '%s\n' \
            "server-build-security=${{ needs.server_build_security.result }}" \
            "client-quality=${{ needs.client_quality.result }}" \
            | tee .ci-evidence/release-artifact/upstream-results.log
          exit 1
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
      - name: Download server artifact
        uses: actions/download-artifact@v8
        with:
          name: server-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/server
      - name: Download client artifact
        uses: actions/download-artifact@v8
        with:
          name: client-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/client
      - name: Verify and assemble exact build outputs
        run: |
          cd .ci-artifacts/server
          sha256sum -c server-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../client
          sha256sum -c client-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/server/server-dist.tgz
          tar -xzf .ci-artifacts/client/client-dist.tgz
          node scripts/verify-dist.mjs
      - name: Package joined release artifact
        run: |
          mkdir -p .ci-artifacts/release
          tar -czf .ci-artifacts/release/release-dist.tgz dist shinobij.client/dist
          cd .ci-artifacts/release
          sha256sum release-dist.tgz > release-dist.tgz.sha256
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\nnode=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$(node -v)" > provenance.txt
      - name: Upload joined release artifact
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          overwrite: true
          path: .ci-artifacts/release/
          if-no-files-found: error
          retention-days: 14
      - name: Retain release artifact evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-release-artifact-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/release-artifact/
          if-no-files-found: warn
          retention-days: 14

  release_certification:
    name: CI / release-certification
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/release-certification
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/release-certification/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/release-certification/upstream-results.log
          exit 1
      - name: Install production server dependencies
        run: npm ci --omit=dev 2>&1 | tee .ci-evidence/release-certification/root-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          # "Re-run failed jobs" reuses this SHA/run artifact from an earlier
          # attempt. The checksum, commit, and run ID are the immutable identity;
          # the attempt remains validated provenance, not an equality constraint.
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
          node scripts/verify-dist.mjs
      - name: Fresh-account and combat release certification
        run: npm run certify:release 2>&1 | tee .ci-evidence/release-certification/certification.log
      - name: Retain release certification evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-release-certification-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/release-certification/
          if-no-files-found: warn
          retention-days: 14

  concurrency_smoke:
    name: CI / concurrency-smoke
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/concurrency-smoke
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/concurrency-smoke/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/concurrency-smoke/upstream-results.log
          exit 1
      - name: Install production server dependencies
        run: npm ci --omit=dev 2>&1 | tee .ci-evidence/concurrency-smoke/root-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
          node scripts/verify-dist.mjs
      - name: Local concurrent-player smoke
        run: npm run soak:smoke 2>&1 | tee .ci-evidence/concurrency-smoke/soak.log
      - name: Retain concurrency evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-concurrency-smoke-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/concurrency-smoke/
          if-no-files-found: warn
          retention-days: 14

  e2e_responsive_matrix:
    name: CI / e2e-responsive / ${{ matrix.shard }}-of-3
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    # Run 35326317976 reached only 824/875 tests on shard 2 before the 29m
    # ceiling, including 6m of required Stronghold audits. Split the expanded
    # suite three ways so those audits and every browser check retain headroom
    # without raising the timeout or dropping coverage.
    timeout-minutes: 29
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\nshard=%s-of-3\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "${{ matrix.shard }}" > .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/upstream-results.log
          exit 1
      # Ranked browser fixtures need root storage packages. Stronghold QA also
      # uses the locked root tsx/esbuild tooling to build its disposable fixture.
      - name: Install server runtime and QA dependencies
        run: npm ci 2>&1 | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/root-install.log
      - name: Install client dependencies
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/client-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify and extract exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
      - name: Install release browsers
        run: npx --prefix shinobij.client playwright install --with-deps chromium firefox webkit 2>&1 | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/browser-install.log
      # Surface focused policy/lifecycle failures before the long browser matrix.
      - name: Verify built CSP in Chromium and WebKit
        if: ${{ matrix.shard == 1 }}
        run: node --test scripts/check-http-security-browser.mjs 2>&1 | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/csp-browser.log
      - name: Stronghold request lifecycle and recovery
        # Keep all Stronghold modes together on shard 2; the third Playwright
        # shard frees time for these audits within the existing 29m limit.
        if: ${{ matrix.shard == 2 }}
        env:
          NODE_ENV: test
          SHINOBIX_QA_MEMORY_KV: '1'
        working-directory: shinobij.client
        run: node scripts/stronghold-browser-qa.mjs --output=../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold 2>&1 | tee ../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold.log
      # Run the Stronghold modes sequentially on that same shard:
      # all Stronghold modes build into the same disposable fixture directory.
      - name: Stronghold dismissal and settlement races
        if: ${{ matrix.shard == 2 }}
        env:
          NODE_ENV: test
          SHINOBIX_QA_MEMORY_KV: '1'
        working-directory: shinobij.client
        run: node scripts/stronghold-browser-qa.mjs --dismissal --output=../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold-dismissal 2>&1 | tee ../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold-dismissal.log
      - name: Stronghold resource lifecycle
        if: ${{ matrix.shard == 2 }}
        env:
          NODE_ENV: test
          SHINOBIX_QA_MEMORY_KV: '1'
        working-directory: shinobij.client
        run: node scripts/stronghold-browser-qa.mjs --resources --output=../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold-resources 2>&1 | tee ../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/stronghold-resources.log
      - name: Cross-browser responsive and accessibility smoke
        run: npm run test:e2e --prefix shinobij.client -- --shard=${{ matrix.shard }}/3 2>&1 | tee .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/playwright.log
      - name: Sector controls and spectator integration
        if: ${{ matrix.shard == 1 }}
        working-directory: shinobij.client
        run: node node_modules/@playwright/test/cli.js test --config playwright.sector-hud.config.ts --project chromium 2>&1 | tee ../.ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/sector-hud.log
      - name: Retain responsive browser evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-responsive-${{ matrix.shard }}-of-3-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-evidence/e2e-responsive-${{ matrix.shard }}-of-3/
            shinobij.client/playwright-report/
            shinobij.client/test-results/
          if-no-files-found: warn
          retention-days: 14

  e2e_responsive:
    name: CI / e2e-responsive
    if: ${{ always() }}
    needs: e2e_responsive_matrix
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every responsive browser shard
        env:
          RESPONSIVE_MATRIX: ${{ needs.e2e_responsive_matrix.result }}
        run: |
          mkdir -p .ci-evidence/e2e-responsive
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/e2e-responsive/provenance.txt
          printf 'e2e-responsive-matrix=%s\n' "$RESPONSIVE_MATRIX" | tee .ci-evidence/e2e-responsive/results.log
          test "$RESPONSIVE_MATRIX" = success
      - name: Retain responsive aggregate evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-responsive-aggregate-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/e2e-responsive/
          if-no-files-found: warn
          retention-days: 14

  e2e_combat_matrix:
    name: CI / e2e-combat / ${{ matrix.shard }}
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 29
    strategy:
      fail-fast: false
      matrix:
        include:
          - shard: chromium
            browser: chromium
            projects: --project=chromium-layout --project=chromium-dpr125 --project=chromium-dpr15 --project=chromium-dpr2
          - shard: firefox
            browser: firefox
            projects: --project=firefox-layout
          - shard: webkit
            browser: webkit
            projects: --project=webkit-layout
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            package-lock.json
            shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/e2e-combat-${{ matrix.shard }}
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\nshard=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "${{ matrix.shard }}" > .ci-evidence/e2e-combat-${{ matrix.shard }}/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/e2e-combat-${{ matrix.shard }}/upstream-results.log
          exit 1
      - name: Install production server dependencies
        run: npm ci --omit=dev 2>&1 | tee .ci-evidence/e2e-combat-${{ matrix.shard }}/root-install.log
      - name: Install client dependencies
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/e2e-combat-${{ matrix.shard }}/client-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify and extract exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
          node scripts/verify-dist.mjs
      - name: Install combat browsers
        run: npx --prefix shinobij.client playwright install --with-deps ${{ matrix.browser }} 2>&1 | tee .ci-evidence/e2e-combat-${{ matrix.shard }}/browser-install.log
      - name: Combat layout and jutsu-arming matrix
        env:
          COMBAT_LAYOUT_CAPTURE_PHASE: after
          COMBAT_LAYOUT_STRICT: "1"
          COMBAT_LAYOUT_ARTIFACT_ROOT: ${{ github.workspace }}/shinobij.client/test-results/combat-layout/captures/${{ matrix.shard }}
        run: npm run test:e2e:combat-layout --prefix shinobij.client -- ${{ matrix.projects }} 2>&1 | tee .ci-evidence/e2e-combat-${{ matrix.shard }}/playwright.log
      - name: Retain combat browser evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-combat-${{ matrix.shard }}-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-evidence/e2e-combat-${{ matrix.shard }}/
            shinobij.client/playwright-report/
            shinobij.client/test-results/combat-layout/
          if-no-files-found: warn
          retention-days: 14

  e2e_combat:
    name: CI / e2e-combat
    if: ${{ always() }}
    needs: e2e_combat_matrix
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every combat browser shard
        env:
          COMBAT_MATRIX: ${{ needs.e2e_combat_matrix.result }}
        run: |
          mkdir -p .ci-evidence/e2e-combat
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/e2e-combat/provenance.txt
          printf 'e2e-combat-matrix=%s\n' "$COMBAT_MATRIX" | tee .ci-evidence/e2e-combat/results.log
          test "$COMBAT_MATRIX" = success
      - name: Retain combat aggregate evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-combat-aggregate-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/e2e-combat/
          if-no-files-found: warn
          retention-days: 14

  e2e_warfront:
    name: CI / e2e-warfront
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    # This is a seven-project browser matrix that also renders eight real skinned
    # rigs, so it belongs on the same 29-minute budget as the other two browser
    # matrices rather than the 20 it was given: observed green runs already take
    # 14-17 minutes, which left the job less headroom than one slow runner uses.
    timeout-minutes: 29
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/e2e-warfront
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/e2e-warfront/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/e2e-warfront/upstream-results.log
          exit 1
      - name: Install client dependencies
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/e2e-warfront/client-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify and extract exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
      - name: Install Warfront browser
        run: npx --prefix shinobij.client playwright install --with-deps chromium 2>&1 | tee .ci-evidence/e2e-warfront/browser-install.log
      - name: Warfront positional-mode browser matrix
        run: npm run test:e2e:warfront --prefix shinobij.client 2>&1 | tee .ci-evidence/e2e-warfront/playwright.log
      - name: Retain Warfront browser evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-warfront-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-evidence/e2e-warfront/
            shinobij.client/playwright-report/
            shinobij.client/test-results/
            .playwright-mcp/aaa-adaptive/
          if-no-files-found: warn
          retention-days: 14

  e2e_village_stores:
    name: CI / e2e-village-stores
    needs: release_artifact
    if: ${{ always() }}
    runs-on: ubuntu-latest
    # The recovery matrix exercises real admission timers on both viewports in
    # addition to the existing economy, Academy and route journeys.
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: shinobij.client/package-lock.json
      - name: Initialize evidence
        run: |
          mkdir -p .ci-evidence/e2e-village-stores
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/e2e-village-stores/provenance.txt
      - name: Require successful release artifact
        if: ${{ needs.release_artifact.result != 'success' }}
        run: |
          echo "release-artifact=${{ needs.release_artifact.result }}" | tee .ci-evidence/e2e-village-stores/upstream-results.log
          exit 1
      # playwright.live.config.ts boots the REAL Express server
      # (node ../dist/server.js), so this job needs the ROOT runtime deps —
      # api/_force-ipv4.ts and api/_storage.ts both pull in undici. Cloned
      # from e2e-warfront, which only serves a static vite preview and
      # therefore never needed them; without this the web server dies with
      # "Cannot find module 'undici'" before a single spec runs.
      - name: Install production server dependencies
        run: npm ci --omit=dev 2>&1 | tee .ci-evidence/e2e-village-stores/root-install.log
      - name: Install client dependencies
        run: npm ci --prefix shinobij.client 2>&1 | tee .ci-evidence/e2e-village-stores/client-install.log
      - name: Download joined release artifact
        uses: actions/download-artifact@v8
        with:
          name: release-dist-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/release
      - name: Verify and extract exact release artifact
        run: |
          cd .ci-artifacts/release
          sha256sum -c release-dist.tgz.sha256
          grep -Fx "sha=$GITHUB_SHA" provenance.txt
          grep -Fx "run_id=$GITHUB_RUN_ID" provenance.txt
          artifact_attempt="$(sed -n 's/^run_attempt=//p' provenance.txt)"
          [[ "$artifact_attempt" =~ ^[1-9][0-9]*$ ]]
          (( artifact_attempt <= GITHUB_RUN_ATTEMPT ))
          cd ../..
          tar -xzf .ci-artifacts/release/release-dist.tgz
      - name: Install live Express browser
        run: npx --prefix shinobij.client playwright install --with-deps chromium 2>&1 | tee .ci-evidence/e2e-village-stores/browser-install.log
      # Keep the required check name stable while covering the shared server
      # routing and save lifecycle used by the economy and Academy journeys.
      - name: Live Express economy, Academy persistence and route wiring
        run: npm run test:e2e:live --prefix shinobij.client -- village-stores-express.spec.ts first-session-onboarding-express.spec.ts server-route-smoke-express.spec.ts --project=chromium-desktop-live 2>&1 | tee .ci-evidence/e2e-village-stores/playwright.log
      - name: Live Express defeat and daily reward recovery on desktop and mobile
        run: npm run test:e2e:live --prefix shinobij.client -- first-defeat-recovery-express.spec.ts daily-login-recovery-express.spec.ts --project=chromium-desktop-live --project=chromium-mobile-live --workers=2 --output=test-results/defeat-recovery-ci 2>&1 | tee .ci-evidence/e2e-village-stores/defeat-recovery.log
      # Its own step, so its own server: registration is limited to 25 accounts
      # per IP per 15 minutes, and the recovery matrix above already registers 18.
      - name: Live Express hospital ward and roaming Weekly Boss on desktop and mobile
        run: npm run test:e2e:live --prefix shinobij.client -- mmorpg-behaviors-express.spec.ts --project=chromium-desktop-live --project=chromium-mobile-live --workers=2 --output=test-results/mmo-behaviors-ci 2>&1 | tee .ci-evidence/e2e-village-stores/mmo-behaviors.log
      - name: Live Express authored missions on desktop and mobile
        run: npm run test:e2e:live --prefix shinobij.client -- authored-missions-express.spec.ts --project=chromium-desktop-live --project=chromium-mobile-live --output=test-results/authored-missions-ci 2>&1 | tee .ci-evidence/e2e-village-stores/authored-missions.log
      - name: Live Express PvP cancellation, settlement, ranked and Kage journeys
        env:
          LIVE_E2E_REALTIME: '1'
        run: npm run test:e2e:live --prefix shinobij.client -- pvp-lifecycle-express.spec.ts ranked-queue-express.spec.ts kage-challenge-express.spec.ts --project=chromium-desktop-live --project=chromium-mobile-live --output=test-results/pvp-lifecycle-ci 2>&1 | tee .ci-evidence/e2e-village-stores/pvp-lifecycle.log
      - name: Live Express Exchange and sector-war player journeys
        run: npm run test:e2e:live --prefix shinobij.client -- sunscar-exchange-express.spec.ts sector-war-express.spec.ts --project=chromium-desktop-live --retries=1 --output=test-results/economy-war-journeys-ci 2>&1 | tee .ci-evidence/e2e-village-stores/economy-war-journeys.log
      - name: Retain Village Stores browser evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-e2e-village-stores-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-evidence/e2e-village-stores/
            shinobij.client/playwright-report/
            shinobij.client/test-results/
          if-no-files-found: warn
          retention-days: 14

  test_build:
    name: CI / test-build
    if: ${{ always() }}
    needs:
      - server_contracts
      - server_build_security
      - client_quality
      - release_artifact
      - release_certification
      - concurrency_smoke
      - e2e_responsive
      - e2e_combat
      - e2e_warfront
      - e2e_village_stores
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every split gate
        env:
          SERVER_CONTRACTS: ${{ needs.server_contracts.result }}
          SERVER_BUILD_SECURITY: ${{ needs.server_build_security.result }}
          CLIENT_QUALITY: ${{ needs.client_quality.result }}
          RELEASE_ARTIFACT: ${{ needs.release_artifact.result }}
          RELEASE_CERTIFICATION: ${{ needs.release_certification.result }}
          CONCURRENCY_SMOKE: ${{ needs.concurrency_smoke.result }}
          E2E_RESPONSIVE: ${{ needs.e2e_responsive.result }}
          E2E_COMBAT: ${{ needs.e2e_combat.result }}
          E2E_WARFRONT: ${{ needs.e2e_warfront.result }}
          E2E_VILLAGE_STORES: ${{ needs.e2e_village_stores.result }}
        run: |
          mkdir -p .ci-evidence/test-build
          printf 'sha=%s\nrun_id=%s\nrun_attempt=%s\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" > .ci-evidence/test-build/provenance.txt
          printf '%s\n' \
            "server-contracts=$SERVER_CONTRACTS" \
            "server-build-security=$SERVER_BUILD_SECURITY" \
            "client-quality=$CLIENT_QUALITY" \
            "release-artifact=$RELEASE_ARTIFACT" \
            "release-certification=$RELEASE_CERTIFICATION" \
            "concurrency-smoke=$CONCURRENCY_SMOKE" \
            "e2e-responsive=$E2E_RESPONSIVE" \
            "e2e-combat=$E2E_COMBAT" \
            "e2e-warfront=$E2E_WARFRONT" \
            "e2e-village-stores=$E2E_VILLAGE_STORES" \
            | tee .ci-evidence/test-build/results.log
          test "$SERVER_CONTRACTS" = success
          test "$SERVER_BUILD_SECURITY" = success
          test "$CLIENT_QUALITY" = success
          test "$RELEASE_ARTIFACT" = success
          test "$RELEASE_CERTIFICATION" = success
          test "$CONCURRENCY_SMOKE" = success
          test "$E2E_RESPONSIVE" = success
          test "$E2E_COMBAT" = success
          test "$E2E_WARFRONT" = success
          test "$E2E_VILLAGE_STORES" = success
      - name: Retain compatibility aggregate evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          include-hidden-files: true
          name: evidence-test-build-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .ci-evidence/test-build/
          if-no-files-found: warn
          retention-days: 14
