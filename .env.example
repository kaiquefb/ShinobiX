# ─────────────────────────────────────────────────────────────────────────────
# ShinobiX environment variables (template — no real values, safe to commit).
#
# Local dev:  copy to `.env` (loaded by app.js via dotenv).
# Railway / Render / Fly / VPS:  set these in the platform's dashboard/secrets,
#                                NOT in a committed file.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database — pick ONE path ─────────────────────────────────────────────────
# Recommended off-Vercel (Railway/VPS/cPanel): direct Postgres pool.
# When DATABASE_URL is set and the app is NOT running on Vercel, the server uses
# the pg connection pool — the same, battle-tested code path as cPanel
# (api/_storage.ts:712). Paste your Supabase "Connection string" here; the app
# auto-strips sslmode/pgbouncer params, so the pooler URI works as-is.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres

# Alternative: Supabase REST API path. Used ONLY if DATABASE_URL is unset.
# SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Auth / secrets (required) ────────────────────────────────────────────────
# Long random string. Enables fast stateless session tokens; WITHOUT it every
# authenticated request falls back to a ~100ms scrypt verify. Rotating this value
# invalidates all existing sessions (logs everyone out).
SESSION_SECRET=change-me-to-a-long-random-string

# ── Google sign-in (optional; the whole feature is inert without it) ─────────
# Server-side OAuth 2.0 authorization-code flow — no client SDK, no extra
# dependency (see api/_google-auth.ts). Create an OAuth client of type "Web
# application" in the Google Cloud Console, then paste its id and secret here.
#
# GOOGLE_REDIRECT_URI must match a URI registered on that client EXACTLY, and
# must be the apex host: the canonical-domain middleware deliberately does not
# normalise /api paths between the apex and www (api/_canonical-domain.ts), so a
# www value would silently differ per visitor.
#
# Google sign-in also requires SESSION_SECRET. A Google account holds no
# password, so without session tokens it could be created and then never entered
# — the server refuses to start the flow at all in that state rather than mint an
# unusable account.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://shinobijourney.com/api/auth/google/callback
# Where the OAuth callback bounces the browser back to. Defaults to "/".
GOOGLE_APP_RETURN_URL=/
# Kill switch. Set to 1 to hide and refuse Google sign-in without unsetting the
# credentials above.
DISABLE_GOOGLE_AUTH=
# Kill switch for guest play (a character with no password, playable instantly).
# Guest play also requires SESSION_SECRET, for the same reason as Google.
DISABLE_GUEST_PLAY=
# Guest characters are RECLAIMED after 14 days of inactivity by the daily cron.
# The sweep reports what it would delete and deletes NOTHING until this is 1 —
# read one night's log before switching it on.
GUEST_SWEEP_ENABLED=

# Admin panel password.
ADMIN_PASSWORD=change-me

# Content-moderation admin password (bloodline/item review, etc.).
ADMIN_CONTENT_PASSWORD=change-me

# Admin session token secret (Phase 4). When set, /api/admin-auth mints a short-
# lived (12h) signed session token at login and the client sends `x-admin-token`
# instead of forwarding the reusable admin password on every request. INERT WHEN
# UNSET: admin auth keeps using the password path unchanged, so leaving this blank
# is safe. Must be high-entropy; never committed; distinct from SESSION_SECRET.
ADMIN_SESSION_SECRET=
# Bump this integer (any new value) and redeploy to instantly revoke every
# outstanding admin session token. Defaults to 0.
ADMIN_SESSION_EPOCH=0
# Set to 1 ONLY after confirming the admin panel is issuing/using tokens: it makes
# the server REJECT the reusable-password admin path (x-admin-password), leaving
# tokens as the only accepted admin credential. Default off so enabling admin
# sessions can never lock a single-operator deployment out mid-migration.
ADMIN_STRICT_TOKEN_ONLY=0

# ── Recommended ──────────────────────────────────────────────────────────────
# The daily save-snapshot backup runs in-process at 03:00 UTC and performs a
# durable-marker catch-up on boot when no complete run occurred in 26 hours.
# CRON_SECRET guards GET /api/cron/snapshot-saves so an independent external
# scheduler can provide an additional firing. Production should use that
# independent schedule as well as the in-process safety net.
CRON_SECRET=change-me

# Secret guarding POST /restart (graceful process restart endpoint).
RESTART_TOKEN=change-me

# Protects the storage-mutating /health?deep=1 and /health/db readiness probe.
# Shallow /health remains public for the platform liveness check. The release
# checker reads this same variable and sends it as a Bearer token.
HEALTH_DEEP_TOKEN=change-me-to-an-independent-random-string
REQUEST_SLO_P95_MS=1500
REQUEST_SLO_MAX_5XX_RATE=0.02
REQUEST_SLO_MIN_REQUESTS=20
# Expensive rolling percentile aggregation is evaluated at most every 15s.
# REQUEST_SLO_EVALUATION_INTERVAL_MS=15000

# Make deep release health fail when no complete snapshot run is newer than 26h.
# Enable on production after the first successful run and monitor it externally.
REQUIRE_FRESH_BACKUP=1

# Railway sends SIGTERM during replacement. Set this platform variable to at
# least 10 so the server's graceful four-second drain can finish in-flight
# save/reward requests before Railway sends SIGKILL.
RAILWAY_DEPLOYMENT_DRAINING_SECONDS=10

# Emergency launch controls. Leave every switch unset during normal operation;
# set the required value to exactly 1 and redeploy when responding to an incident.
# MAINTENANCE_MODE and FREEZE_ECONOMY_REWARDS gate Express player traffic only;
# they do not quiesce GET-side effects, scheduled jobs, realtime, or storage.
# MAINTENANCE_MODE=1
# DISABLE_NEW_REGISTRATIONS=1
# FREEZE_ECONOMY_REWARDS=1
# DISABLE_SCHEDULED_JOBS=1
# Stop the presence snapshot timer and the one-second game loop (including
# sleeper-camp/travel-lease materialization). Socket.IO is separate below.
# DISABLE_PRESENCE_STATE_JOBS=1

# ── Optional ─────────────────────────────────────────────────────────────────
# Only needed if the in-game AI image generation endpoint is used.
# OPENAI_API_KEY=sk-...

# Optional Sentry error reporting is OFF when its DSN is absent. Browser
# environment labels come from the Vite build mode; server labels use NODE_ENV.
# SENTRY_DSN=https://public@example.ingest.sentry.io/1
# VITE_SENTRY_DSN=https://public@example.ingest.sentry.io/1
# VITE_SENTRY_RELEASE=<git-commit-or-release-name>
# VITE_BUILD_COMMIT=<40-character-git-commit>
#
# Optional aggregate-only product analytics pilot. All gates are OFF by default.
# No SDK, autocapture, replay, cookies, person profiles, or stable player identity
# are used. Pick the exact PostHog region shown by the project dashboard.
# PRODUCT_ANALYTICS_ENABLED=1
# PRODUCT_ANALYTICS_PROVIDER=posthog
# POSTHOG_PROJECT_KEY=phc_your_public_project_token
# POSTHOG_HOST=https://us.i.posthog.com
# VITE_PRODUCT_ANALYTICS_ENABLED=1
# VITE_PRODUCT_ANALYTICS_PROVIDER=posthog
# VITE_POSTHOG_KEY=phc_your_public_project_token
# VITE_POSTHOG_HOST=https://us.i.posthog.com

# ── Realtime / background-job toggles (optional) ─────────────────────────────
# Disable the Socket.IO presence layer (clients fall back to the HTTP heartbeat).
# Leave unset on the primary instance so realtime is ON.
# DISABLE_REALTIME=1
#
# Skip the in-process daily save-snapshot on THIS instance. Set on any SECONDARY
# instance (e.g. cPanel) so only the primary (Railway) runs the nightly backup —
# the 20h dedup window makes a double run harmless, this just avoids a redundant
# keyspace scan. Leave unset on the primary so backups run.
# DISABLE_SNAPSHOT_CRON=1
#
# Stop only the five-minute durable-settlement stale scanner. The scanner never
# moves value; it marks old non-terminal journals for operator review. Leave it
# enabled on the primary unless its keyspace scan is part of an active incident.
# DISABLE_SETTLEMENT_RECONCILIATION=1
#
# Optional deep-probe single-flight cache. Bounds repeated KV/disk writes while
# keeping readiness current. Default 15000; allowed range is 1000..60000 ms.
# DEEP_HEALTH_CACHE_MS=15000
#
# Client (build-time) — point the game Socket.IO connection at a remote backend
# for cross-origin dev. Unset = same-origin (correct in production).
# VITE_REALTIME_URL=https://your-backend.example.com

# ── Postgres connection tuning (pg pool path only) ───────────────────────────
# Pool size PER process. Default 5 (fine for cPanel's many worker processes).
# A single always-on Railway/VPS instance serving all heartbeat writes wants
# more — set ~15 on Railway.
# PG_POOL_MAX=15
#
# SSL. Default = TLS on (required by Supabase and Railway's PUBLIC proxy URL).
# Set to "disable" ONLY when connecting over Railway's PRIVATE network
# (host ends in .railway.internal), which serves plaintext on an isolated
# per-project overlay and rejects an SSL handshake. Do NOT disable SSL on any
# public/internet connection string.
# PG_SSL=disable

# ── Storage mode: where save:* / shared:images* keys live ────────────────────
#
#  A) Supabase-only — **PRODUCTION since the cPanel overlay retirement,
#     2026-07-17** (docs/RETIRE_CPANEL_RUNBOOK.md). Every key — including player
#     saves and shared images — lives on the base Postgres/Supabase store. Leave
#     DISK_KV_DIR, KV_PROXY_*, and REQUIRE_DISK_OVERLAY unset. The
#     /health?deep=1 probe must report saveStore="base-store".
#
#  B) cPanel disk overlay (RETIRED; rollback-only during the soak window).
#     save: / shared:images / shared:imgfields keys route to the cPanel disk.
#     save-snapshot: deliberately stays on the base Postgres/Supabase store,
#     keeping recovery copies outside the live-save disk/proxy failure domain.
#     Railway reaches the overlay over HTTP via the KV proxy; cPanel reads the
#     disk directly. Only re-enable this as the documented rollback — and
#     reconcile saves written to Postgres first (see the runbook).
#         # On Railway (remote → cPanel proxy):
#         KV_PROXY_URL=https://theravensark.com/api/kv
#         KV_PROXY_TOKEN=<shared secret, identical to the cPanel box's KV token>
#         # On the cPanel box itself (direct disk; NOT on Railway):
#         DISK_KV_DIR=/home/USER/kv-storage
#
#  ‼ Fail-closed guard for mode B ONLY. On ANY instance serving /api/save/* from
#  the overlay, set REQUIRE_DISK_OVERLAY=1: the server then REFUSES TO BOOT if
#  the overlay env is missing instead of silently falling back to the base
#  store. In mode A (current production) it MUST stay unset — setting it with no
#  overlay configured refuses to boot by design.
# REQUIRE_DISK_OVERLAY=1
#
# One-shot migration safety latch. Set only while legacy base writers are fully
# stopped and an administrator is running the live /api/admin/migrate-kv move;
# unset it immediately afterward. Dry runs do not require this acknowledgement.
# KV_MIGRATION_WRITE_FROZEN=1
#
# cPanel-only Supabase DNS bypass. Leave unset on Railway/Docker. If CageFS cannot
# resolve outbound Supabase DNS, set BOTH values after resolving the current host
# externally. There is no source fallback IP; a rotated IP must be a config change.
# SUPABASE_DNS_BYPASS=1
# SUPABASE_DNS_HOST=YOUR_PROJECT.supabase.co
# SUPABASE_HARDCODED_IP=203.0.113.10
#
# Public-beta reward integrity gates. Leave these unset unless the matching
# combat path has been moved to server-authoritative settlement.
# ENABLE_WEEKLY_BOSS_CLIENT_DAMAGE=1
# ENABLE_CLIENT_TRUSTED_COMBAT_MISSION_REWARDS=1
#
# Battle Towers emergency launch stop. This blocks only new ready rooms and new
# starts; party polling/joining plus active-run reconnect/action/settlement stay
# available so in-flight sessions and durable receipts can recover.
# TOWER_MODE_DISABLED=1
#
# Staged raw-save ownership boundary. Leave unset until the remaining legacy
# progression writers, forged-item backfill, and exact-candidate staging journey
# have passed. Inventory/equipment minting is blocked even before this cutover.
# Enabling the wider boundary early can roll back legitimate mastery/pet writes.
# STRICT_RAW_SAVE_LEDGER=1
#
# Public-beta content/spend gate. Leave unset so only admins can call
# /api/generate-image. Set only when player AI image generation has moderation,
# budget monitoring, and abuse response staffed.
# ENABLE_PLAYER_AI_IMAGE_GENERATION=1

# ── Live feature switches ────────────────────────────────────────────────────
# These systems are ON BY DEFAULT and are turned off with a DISABLE_ switch —
# there is no ENABLE_ var to set. ENABLE_VILLAGE_WAR and ENABLE_CLAN_BOSS are
# deprecated and read by nothing (api/_release-flags.ts), so setting them does
# nothing; only the kill-switches below have any effect.
# DISABLE_VILLAGE_WAR=1        # Sector War: the war map and sector-war routes, war
#                              # structures/terrain/win conditions, mercenaries, the
#                              # daily war pass, the village tax and world PvP sector
#                              # scoring. NOT gated: war missions, war crates, and the
#                              # all-out village war (it then costs Honor Seals)
#                              # (docs/CONTROLLED_WAR_EVENT_RUNBOOK.md)
# WAR_EVENT_ID=war-2026-10-03  # Optional label stamped on every [war-event] log line
#                              # during a staffed war event; unset it afterwards.
# DISABLE_CLAN_BOSS=1          # Weekly Clan Boss Gauntlet + its cron
# DISABLE_CLAN_BOSS_PARTIES=1  # Party/finder only; preserves the server-owned solo path.
#                              # Required for the initial operation rollout; remove only
#                              # on disposable staging, then retain as the party rollback.
# DISABLE_ANBU_INFILTRATION=1  # ANBU Vault raid (also gated in-game at level 100)
# DISABLE_COMBAT_RECEIPTS=1    # PvP settlement receipts — leave UNSET; disabling
#                              # removes replay protection on combat payouts
# DISABLE_ASSET_META=1
# DISABLE_CURRENCY_WINDOW=1    # per-minute currency-gain window in the save sanitizer
# ALLOW_CLIENT_RYO_DECREASE=1  # ROLLBACK ONLY — lets a generic save LOWER ryo again.
#                              # Ryo is server-owned: saves re-assert the stored balance.
#                              # Set this only if a client-side spend was missed (look for
#                              # "[save] ignored client-originated ryo decrease" in logs).
#
# ── ENABLE_LEGACY — required, and silent when missing ────────────────────────
# The Legacy system (Sage/Trial paths, Hall of Legends, era titles) is the one
# feature that needs an explicit opt-in: api/_legacy-track.ts checks
# ENABLE_LEGACY === '1', and every /legacy/* route 404s without it while the
# client UI stays visible. Unset it and players see a Legacy tab that silently
# fails. The server logs a startup warning when it is missing.
# ENABLE_LEGACY=1

# ── Do NOT set on Railway / containers ───────────────────────────────────────
#   PORT        — injected by the platform; the server reads it automatically.
#   STATIC_DIR  — default already resolves to shinobij.client/dist in the image.
#   DISK_KV_DIR — local-file overlay; cPanel-only (and only for a mode-B
#                 rollback). Containers have an EPHEMERAL filesystem — never
#                 set DISK_KV_DIR on Railway.
#   FORCE_PG_KV — set to 1 only to force the pg path when SUPABASE_URL is also set.
